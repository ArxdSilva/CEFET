class Vehicle(object):
    def __init__(self, comb_cons, comb_inicial, eficiencia):
        self.comb_cons = comb_cons
        self.comb_inicial = comb_inicial
        self.eficiencia = eficiencia

    def mover(self, km):
        rodando = ((km/self.eficiencia) == )
        if :
